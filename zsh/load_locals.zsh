[[ -r ~/.local_profile.zsh ]] && . ~/.local_profile.zsh
[[ -r ~/.local_exports.zsh ]] && . ~/.local_exports.zsh
[[ -r ~/.local_aliases.zsh ]] && . ~/.local_aliases.zsh
[[ -r ~/.local_functions.zsh ]] && . ~/.local_functions.zsh
